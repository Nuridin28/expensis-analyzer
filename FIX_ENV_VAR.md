# 🔧 Исправление ошибки подключения к API

## Проблема
```
POST http://localhost:3001/api/parse-pdf net::ERR_CONNECTION_REFUSED
```

Frontend пытался подключиться к localhost вместо production backend.

## ✅ Решение

Я обновил код в `frontend/src/services/api.js`:
- Теперь в **production** автоматически используется `https://expensis-analyzer.onrender.com`
- В **development** используется `http://localhost:3001`

## 📝 Что нужно сделать

### 1. Закоммитьте и запушьте изменения

```bash
git add frontend/src/services/api.js
git commit -m "Fix API URL for production deployment"
git push
```

### 2. Vercel автоматически сделает новый деплой

После push в GitHub, Vercel автоматически обнаружит изменения и сделает новый деплой.

### 3. (Опционально) Установите переменную окружения в Vercel

Хотя теперь код работает без переменной, рекомендуется установить её для правильной конфигурации:

1. Зайдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект → **Settings** → **Environment Variables**
3. Добавьте:
   - **Key**: `VITE_API_URL`
   - **Value**: `https://expensis-analyzer.onrender.com`
   - Выберите все окружения: Production, Preview, Development
4. Сохраните

### 4. Проверьте работу

После деплоя:
1. Откройте https://expensis-analyzer.vercel.app/
2. Попробуйте загрузить выписку
3. Проверьте консоль браузера (F12) - запросы должны идти на `expensis-analyzer.onrender.com`

## 🎯 Результат

Теперь приложение будет работать даже без переменной окружения, но рекомендуется установить её для правильной конфигурации.

