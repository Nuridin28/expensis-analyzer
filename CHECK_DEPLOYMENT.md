# Чеклист проверки деплоя

## ✅ Статус деплоя

- [x] Backend задеплоен на Render: https://expensis-analyzer.onrender.com
- [x] Frontend задеплоен на Vercel: https://expensis-analyzer.vercel.app/

## 🔍 Проверка конфигурации

### Backend (Render)
- [ ] Backend отвечает на `/api/health` - проверьте: https://expensis-analyzer.onrender.com/api/health
- [ ] Переменные окружения установлены в Render Dashboard:
  - [ ] `DEEPSEEK_API_URL`
  - [ ] `DEEPSEEK_API_KEY`
  - [ ] `NODE_ENV` = `production`

### Frontend (Vercel)
- [ ] Переменная окружения установлена в Vercel Dashboard:
  - [ ] `VITE_API_URL` = `https://expensis-analyzer.onrender.com` (без слеша!)
- [ ] После установки переменной сделан новый деплой (Redeploy)

## 🧪 Функциональное тестирование

1. **Откройте**: https://expensis-analyzer.vercel.app/
2. **Откройте консоль браузера** (F12 → Console)
3. **Проверьте запросы** (F12 → Network):
   - При загрузке выписки запросы должны идти на `expensis-analyzer.onrender.com`
   - Не должно быть ошибок CORS
   - Не должно быть 404 ошибок для API endpoints

4. **Протестируйте функции**:
   - [ ] Загрузка примера выписки (кнопка "Загрузить пример")
   - [ ] Загрузка JSON файла
   - [ ] Загрузка PDF файла
   - [ ] Вставка JSON вручную
   - [ ] Анализ выписки
   - [ ] Отображение результатов (классификация, подписки, прогноз, рекомендации)

## 🐛 Решение проблем

### Проблема: Frontend не может подключиться к backend

**Симптомы**: В консоли браузера ошибки типа "Network Error" или "CORS Error"

**Решение**:
1. Проверьте переменную `VITE_API_URL` в Vercel Dashboard
2. Убедитесь, что значение: `https://expensis-analyzer.onrender.com` (без слеша!)
3. После изменения переменной сделайте Redeploy в Vercel
4. Проверьте, что backend доступен: https://expensis-analyzer.onrender.com/api/health

### Проблема: Backend не отвечает

**Симптомы**: Запросы висят или возвращают timeout

**Решение**:
1. На Free плане Render сервисы "засыпают" после 15 минут неактивности
2. Первый запрос после пробуждения может занять 30-60 секунд
3. Проверьте логи в Render Dashboard → Logs
4. Убедитесь, что backend запущен (Status должен быть "Live")

### Проблема: CORS ошибки

**Симптомы**: В консоли ошибка "Access-Control-Allow-Origin"

**Решение**:
1. Backend уже настроен с `app.use(cors())` - это должно работать
2. Если проблема сохраняется, проверьте логи backend в Render
3. Убедитесь, что запросы идут на правильный URL

### Проблема: Переменная окружения не применяется

**Симптомы**: Frontend все еще использует localhost или старый URL

**Решение**:
1. В Vercel переменные окружения применяются только при сборке
2. После изменения переменной ОБЯЗАТЕЛЬНО сделайте Redeploy
3. Проверьте, что переменная установлена для всех окружений (Production, Preview, Development)

## 📝 Полезные ссылки

- **Frontend**: https://expensis-analyzer.vercel.app/
- **Backend Health Check**: https://expensis-analyzer.onrender.com/api/health
- **Vercel Dashboard**: https://vercel.com/dashboard
- **Render Dashboard**: https://dashboard.render.com/

## 🎯 Быстрая проверка

Выполните в терминале для проверки backend:
```bash
curl https://expensis-analyzer.onrender.com/api/health
```

Должен вернуться JSON:
```json
{"status":"ok","message":"Bank Statement Analyzer API is running"}
```

