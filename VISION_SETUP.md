# Настройка Vision для обработки PDF с изображениями

## ✅ Что добавлено:

1. **Автоматическое определение типа PDF**:
   - Если PDF содержит текст - используется текстовый анализ
   - Если PDF содержит только изображения - конвертируется в изображения и отправляется в Vision API

2. **Конвертация PDF в изображения**:
   - Использует `pdfjs-dist` для парсинга PDF
   - Использует `canvas` для рендеринга страниц в изображения
   - Конвертирует максимум 3 страницы для экономии

3. **Vision API интеграция**:
   - Отправляет изображения в Deepseek с поддержкой vision
   - LLM анализирует изображения и извлекает транзакции
   - Возвращает структурированные данные

## 📦 Установка:

```bash
cd backend

# Обязательные зависимости
npm install pdfjs-dist pdf-parse

# Для конвертации PDF в изображения (требует системные зависимости)
npm install canvas
```

### Системные зависимости для canvas:

**macOS:**
```bash
brew install pkg-config cairo pango libpng jpeg giflib librsvg pixman
```

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev
```

## 🚀 Использование:

1. Установите зависимости
2. Запустите backend: `npm run dev`
3. Загрузите PDF с банковской выпиской
4. Система автоматически определит тип PDF и обработает соответственно

## ⚠️ Примечания:

- Если canvas не установлен, будет ошибка при попытке обработать PDF с изображениями
- Deepseek должен поддерживать vision API (проверьте документацию)
- Размер изображений ограничен для экономии токенов
