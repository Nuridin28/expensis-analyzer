# Поддержка PDF файлов

## ✅ Что добавлено:

### Backend
1. **Новый endpoint**: `/api/parse-pdf`
   - Принимает PDF файл через multipart/form-data
   - Извлекает текст из PDF с помощью `pdf-parse`
   - Использует Deepseek AI для извлечения структурированных данных
   - Возвращает массив транзакций

2. **Зависимости**:
   - `pdf-parse` - для извлечения текста из PDF
   - `multer` - для загрузки файлов (уже был)

### Frontend
1. **Функция `parsePDF`** в `api.js`
   - Отправляет PDF файл на сервер
   - Обрабатывает ответ с извлеченными данными

2. **Обновлен `StatementUpload`**:
   - Поддержка загрузки PDF файлов
   - Автоматический анализ после извлечения данных
   - Индикатор загрузки при обработке PDF
   - Улучшенный UI с информацией о поддерживаемых форматах

## 🚀 Установка и запуск:

```bash
# 1. Установите зависимости backend
cd backend
npm install
npm install pdf-parse  # дополнительная зависимость для PDF

# 2. Запустите backend
npm run dev

# 3. В другом терминале - frontend
cd ../frontend
npm install
npm run dev
```

## 📝 Как использовать:

1. Откройте http://localhost:3000
2. Нажмите "Загрузить файл"
3. Выберите PDF файл с банковской выпиской
4. Дождитесь обработки (извлечение текста + AI анализ)
5. Данные автоматически загрузятся и проанализируются

## ⚠️ Ограничения:

- PDF должен содержать текст (не сканированное изображение)
- Максимальный размер файла: 10MB
- Для сканированных PDF требуется OCR (не поддерживается)
- Качество извлечения зависит от структуры PDF
