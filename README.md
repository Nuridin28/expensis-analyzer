# Анализатор банковских выписок

Супер удобное и красивое веб-приложение для анализа банковских выписок с использованием Deepseek AI.

## Функции

- 📄 **Разбор банковских выписок** - Загрузка и обработка выписок в формате JSON/CSV
- 🏷️ **Классификация трат** - Автоматическая категоризация расходов по категориям
- 💡 **Рекомендации** - Персонализированные советы по оптимизации финансов
- 📈 **Прогнозирование расходов** - Прогноз на следующие 3 месяца
- 🔔 **Анализ подписок** - Обнаружение и анализ регулярных платежей

## Технологии

- **Frontend**: React + Vite + Tailwind CSS
- **Backend**: Node.js + Express
- **AI**: Deepseek API
- **Charts**: Recharts

## Установка и запуск

### Быстрый старт

1. **Установите зависимости для обоих проектов:**
```bash
npm run install:all
```

Или вручную:
```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

2. **Установите дополнительные зависимости для PDF**

Для поддержки PDF файлов (включая сканированные изображения):
```bash
cd backend
npm install pdf-parse pdfjs-dist

# Для обработки PDF с изображениями (опционально, требует системные зависимости):
npm install canvas

# Системные зависимости для canvas (macOS):
brew install pkg-config cairo pango libpng jpeg giflib librsvg pixman

# Или Ubuntu/Debian:
sudo apt-get install build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev
```

**Примечание:** Если `canvas` не установлен, PDF с текстом будут работать, но PDF с изображениями потребуют установки canvas.

3. **Настройте переменные окружения**

Файл `.env` уже создан в папке `backend` с API ключом Deepseek.

4. **Запустите приложение**

В двух отдельных терминалах:

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```
Backend будет доступен на `http://localhost:3001`

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```
Frontend будет доступен на `http://localhost:3000`

Откройте браузер и перейдите на `http://localhost:3000`

### ⚠️ Решение проблем

**Если стили не работают:**
1. Убедитесь, что `frontend/tailwind.config.js` существует ✅ (восстановлен)
2. Остановите frontend сервер (Ctrl+C)
3. Удалите кэш Vite: `rm -rf frontend/node_modules/.vite`
4. Переустановите зависимости: `cd frontend && npm install`
5. Запустите снова: `npm run dev`
6. Проверьте консоль браузера (F12) - не должно быть ошибок загрузки CSS

**Если загрузка файла не работает:**
1. ✅ Улучшена обработка ошибок загрузки файлов
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Убедитесь, что файл в формате JSON или CSV
4. Попробуйте использовать кнопку "Загрузить пример"
5. Или вставьте JSON вручную в текстовое поле
6. Проверьте, что backend сервер запущен на порту 3001
7. Убедитесь, что файл не пустой и не превышает 10MB
8. Для CSV файла убедитесь, что есть заголовки: дата, сумма, операция, детали

## Поддерживаемые форматы

Приложение поддерживает три формата выписок:

### 📄 PDF файлы
- **PDF с текстом**: автоматическое извлечение данных из текста
- **PDF с изображениями (сканированные)**: конвертация страниц в изображения и анализ через Deepseek Vision API
- Автоматическое определение типа PDF и выбор метода обработки
- Требует установки `pdfjs-dist` и `canvas` для обработки изображений

### 📋 JSON файлы
Выписка должна быть массивом объектов со следующими полями:

```json
[
  {
    "дата": "2024-01-15",
    "сумма": -1500,
    "операция": "Покупка",
    "детали": "Супермаркет Ашан"
  },
  {
    "дата": "2024-01-16",
    "сумма": -500,
    "операция": "Оплата",
    "детали": "Netflix подписка"
  }
]
```

Поля:
- **дата** - дата транзакции
- **сумма** - сумма (отрицательная для расходов, положительная для доходов)
- **операция** - тип операции
- **детали** - описание транзакции

### 📊 CSV файлы
Табличный формат с заголовками: дата, сумма, операция, детали

### Пример выписки

В корне проекта есть файл `example-statement.json` с примером данных для тестирования. Вы можете загрузить его через интерфейс приложения или использовать кнопку "Загрузить пример" в интерфейсе.

**Для PDF:** 
- Загрузите PDF файл с банковской выпиской
- Если PDF содержит текст - данные извлекаются напрямую
- Если PDF содержит только изображения - страницы конвертируются в изображения и анализируются через Deepseek Vision API
- Все происходит автоматически, без необходимости выбирать тип PDF

## Структура проекта

```
flutter_without_a_flutter/
├── backend/
│   ├── server.js
│   ├── package.json
│   └── .env
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Sidebar.jsx
│   │   │   ├── StatementUpload.jsx
│   │   │   ├── Dashboard.jsx
│   │   │   ├── Classifications.jsx
│   │   │   ├── Subscriptions.jsx
│   │   │   ├── Forecast.jsx
│   │   │   └── Recommendations.jsx
│   │   ├── services/
│   │   │   └── api.js
│   │   ├── App.jsx
│   │   ├── main.jsx
│   │   └── index.css
│   ├── package.json
│   └── vite.config.js
└── README.md
```

